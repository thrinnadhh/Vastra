name: Temporary customer deep-link lint fix

on:
  push:
    branches:
      - feat/ui-fe-s03-production-deep-links

permissions:
  contents: write

jobs:
  lint-fix:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
        with:
          ref: feat/ui-fe-s03-production-deep-links
      - uses: pnpm/action-setup@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 20.20.2
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml
      - run: pnpm install --frozen-lockfile
      - run: pnpm --filter @vastra/api-client generate
      - run: pnpm lint:fix || true
      - run: rm .github/workflows/temporary-deep-link-lint-fix.yml
      - name: Commit lint fixes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "FE-S03-01 style(customer): apply deep-link lint fixes"
          git push origin HEAD:feat/ui-fe-s03-production-deep-links
