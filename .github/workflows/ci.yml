name: CI

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    name: Verify repository
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Check out repository
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: Install pnpm
        uses: pnpm/action-setup@v6

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 20.20.2
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Check formatting
        run: pnpm format:check

      - name: Validate environment templates
        run: pnpm env:check

      - name: Lint
        run: pnpm lint

      - name: Typecheck
        run: pnpm typecheck

      - name: Run unit tests
        run: pnpm test

      - name: Run integration tests
        run: pnpm test:integration

      - name: Set up Supabase CLI
        uses: supabase/setup-cli@v3
        with:
          version: 2.109.1

      - name: Start Supabase
        run: supabase start

      - name: Reset database and apply migrations
        run: supabase db reset --local

      - name: Verify migration history
        run: supabase migration list --local

      - name: Run S10-08 pgTAP contracts
        run: supabase test db supabase/tests/0065_sprint10_admin_return_decision.test.sql --local

      - name: Run S10-09 pgTAP contracts
        run: supabase test db supabase/tests/0066_sprint10_refund_execution.test.sql --local

      - name: Run S10-12 pgTAP contracts
        run: supabase test db supabase/tests/0067_sprint10_finance_closure.test.sql --local

      - name: Stop Supabase
        if: always()
        run: supabase stop
