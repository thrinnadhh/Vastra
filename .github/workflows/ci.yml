name: CI

on:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  diagnose:
    name: Diagnose PR 53 database
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: false
      - uses: pnpm/action-setup@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 20.20.2
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml
      - run: pnpm install --frozen-lockfile
      - uses: supabase/setup-cli@v3
        with:
          version: 2.109.1
      - name: Capture database report
        id: database
        continue-on-error: true
        shell: bash
        run: |
          set -o pipefail
          pnpm db:test 2>&1 | tee /tmp/pr53-database.log
      - name: Upload database report
        uses: actions/upload-artifact@v4
        with:
          name: pr53-database-diagnostics
          path: /tmp/pr53-database.log
          retention-days: 1
      - name: Preserve database failure
        if: steps.database.outcome == 'failure'
        run: exit 1
