name: Publish FE-S05 ticket branch

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  publish:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo 'FE-S05-02 repair and evidence have already been published.'
