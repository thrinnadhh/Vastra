export const CUSTOMER_DISCOVERY_LIMITS = Object.freeze({
  searchPageSize: 20,
  shopCataloguePageSize: 20,
  nearbyShopLimit: 50,
  recentSearchLimit: 5,
});

export const CUSTOMER_DISCOVERY_MEDIA = Object.freeze({
  productHeroHeight: 360,
});
